<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bassoum.marketplace">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".activities.BaseApplication"
        >
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.RegisterActivity"
            android:label="@string/title_activity_register"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.ProduitActivity">
            android:theme="@style/AppTheme.NoActionBar"
        </activity>
        <activity android:name=".activities.RobeActivity" />
        <activity android:name=".activities.BotteActivity" />
        <activity android:name=".activities.ChemiseActivity" />
        <activity android:name=".activities.PortableActivity" />
        <activity android:name=".activities.MontreActivity" />
        <activity android:name=".activities.BracellerActivity"></activity>
    </application>

</manifest>